package main

import (
    /*"fmt"
    "image"
    "os"
    "github.com/twmb/algoimpl/go/graph"*/

)
import _"image/jpeg"


func main() {
    
    
    P:=make([] *Solution,1,2)
    F:=nonDominatedRank(P)
    nextGen:= make([] *Solution, 1, 1)
    for i:=0; i<N; i++{
        r:=rand.New(rand.NewSource(time.Now().UnixNano()))
        index = math.Pow(r.Float64,2)*N
        child:=mutate(
    }
    

}











